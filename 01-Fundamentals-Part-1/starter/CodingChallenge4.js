/* //bill values 275, 40 and 430

let tip;
let billValue = 275;

tip = (billValue>=50 && billValue <=300) ? 0.15*billValue : 0.2*billValue;

console.log(`The bill was ${billValue} , the tip was `
+ `${tip} and total value is ${billValue+tip}`);


 */