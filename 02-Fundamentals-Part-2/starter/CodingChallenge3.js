/* const Mark = {
    mass : 78,
    height: 1.69,
    name: 'Mark',
    calcBMI: function(){
        return this.mass/(this.height**2);
    }
}

const John = {
    mass : 92,
    height: 1.95,
    name: 'John',
    calcBMI: function(){
        return this.mass/(this.height**2);
    }
}

const markBMI = Mark.calcBMI();
const johnBMI = John.calcBMI();

if(markBMI>johnBMI){
    console.log(`${Mark.name}'s bmi ${markBMI} is higher than ${John.name}'s ${johnBMI}`);
}else if(johnBMI>markBMI){
    console.log(`${John.name}'s bmi ${johnBMI} is higher than ${Mark.name}'s ${markBMI}`)
}else{
    console.log('Both bmi are the same');
} */